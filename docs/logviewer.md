# Log Viewer

Log viewing is explicit and on-demand. Search and level filters apply after lines are read. Known
secret assignment formats are redacted before rendering or export; callers must still avoid
logging secrets in the first place.
