# Health

Health checks are supplied as `HealthStatus` values from the core observability module. The
Developer Platform only displays supplied results, so it cannot alter lifecycle, scheduler, or
application state.
