# Architecture Inspector

`ArchitectureInspector` evaluates public service descriptors and reports non-invasive warnings.
It never accesses the container's internal registration map or resolves services.
